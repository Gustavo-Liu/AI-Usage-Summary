# AI Year-In-Review

一个隐私优先的 AI 对话分析工具，将 ChatGPT 对话记录转换为可视化的年度回顾报告。

## 功能特性

### 📊 严肃硬核指标 (8个)
1. **基础统计** - 总对话数、总消息数、总字数、最活跃时间
2. **大脑活跃热力图** - 24小时 x 7天的活动热力图
3. **年度知识图谱** - 高频关键词提取（Top 30）
4. **思维深潜度** - 对话轮数分布（浅层/中层/深层）
5. **累计阅读量** - AI 回复总字数，换算成书籍概念
6. **输入输出比** - 你的提问 vs AI 回复的比例
7. **语言偏好分布** - 中英文使用比例
8. **最长思维马拉松** - 持续时间最长或轮数最多的对话

### 😼 有趣/性格指标 (2个)
9. **AI 礼貌指数** - 统计 "请"、"谢谢"、"帮忙" 等礼貌用语
10. **用户画像徽章** - 深夜哲学家/晨间实干家/全勤卷王等个性化标签

## 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

**依赖说明：**
- `jieba` - 中文分词（必需）
- `nltk` - 英文自然语言处理（可选，但推荐）
- `langdetect` - 语言检测（可选，但推荐）

### 2. 准备数据

从 ChatGPT 导出你的对话数据，保存为 `conversation.json` 文件。

### 3. 运行分析

```bash
python3 analyze_conversations.py
```

或者指定输入输出文件：

```bash
python3 analyze_conversations.py conversation.json summary.json
```

### 4. 查看结果

分析完成后，会生成 `summary.json` 文件，包含所有分析结果。

## 输出格式

`summary.json` 包含以下结构：

```json
{
  "summary_stats": {
    "total_conversations": 800,
    "total_messages": 2333,
    "total_user_characters": 2624374,
    "total_assistant_characters": 6368413,
    "books_equivalent": {
      "三体": 31.8,
      "哈利波特": 63.7,
      "小王子": 318.4
    }
  },
  "brain_activity_heatmap": {
    "heatmap_data": [[hour, weekday, count], ...],
    "hour_distribution": {...},
    "weekday_distribution": {...}
  },
  "keywords": {
    "top_keywords": [...],
    "total_unique_keywords": 150
  },
  "deep_dive_index": {...},
  "directors_ratio": {...},
  "linguistic_profile": {...},
  "marathon_session": {...},
  "monthly_focus": [...],
  "politeness_score": {...},
  "persona_badge": {...},
  "metadata": {
    "generated_at": "2025-01-09T...",
    "version": "1.0"
  }
}
```

## 技术栈

### 分析脚本
- **Python 3.7+**
- **jieba** - 中文分词和关键词提取
- **nltk** - 英文分词和停用词过滤
- **langdetect** - 语言检测

### 未来前端（待实现）
- **React/Vue** - 前端框架
- **ECharts/D3.js** - 数据可视化
- **Web Workers** - 客户端数据处理
- **Canvas API** - 生成分享卡片

## 项目结构

```
AI User Map/
├── conversation.json          # 输入的 ChatGPT 对话数据
├── analyze_conversations.py   # 分析脚本
├── summary.json               # 分析结果输出
├── requirements.txt          # Python 依赖
├── Instruction               # 项目需求文档
└── README.md                # 本文件
```

## 隐私保护

- ✅ 所有数据处理在本地完成
- ✅ 不会上传任何数据到服务器
- ✅ 生成的 summary.json 可以安全分享（不包含原始对话内容）

## Web 应用启动

### 1. 安装 Web 应用依赖

```bash
pip install -r requirements.txt
```

### 2. 配置环境变量

确保 `.env` 文件存在并包含：

```env
OPENAI_API_KEY=your_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
```

**注意**: 如果使用自定义 OpenAI 兼容 API，请修改 `OPENAI_BASE_URL` 为你的 API 端点。

### 3. 启动应用

**方式一：使用启动脚本**
```bash
./start.sh
```

**方式二：直接使用 uvicorn**
```bash
python3 -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 4. 访问应用

打开浏览器访问: http://localhost:8000

## API 端点

### GET `/api/summary`
获取分析摘要数据（JSON 格式）

### POST `/chat`
聊天端点，调用 OpenAI 兼容的聊天补全 API

**请求体:**
```json
{
  "user_message": "你的问题"
}
```

**响应:**
```json
{
  "content": "AI 的回复内容"
}
```

### GET `/health`
健康检查端点

## 下一步

1. ✅ 本地分析脚本（已完成）
2. ✅ FastAPI Web 应用（已完成）
3. ⏳ 添加动画效果
4. ⏳ 生成可分享的年度回顾卡片

## 许可证

MIT License

